# Troubleshooting

If completion state looks stale, clear this site's local storage in the browser and refresh. If task data does not render, run `python3 scripts/validate_tasks.py data/tasks.json` and inspect the browser console for the fetch error.
