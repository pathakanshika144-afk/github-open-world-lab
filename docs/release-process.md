# Release process

Run the quality commands, update `CHANGELOG.md`, create a release branch, and merge only after required checks and independent approval pass. Tag release candidates as pre-releases, then create the immutable stable tag after main succeeds.
